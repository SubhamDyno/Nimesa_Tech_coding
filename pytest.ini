[pytest]
log_cli=true
log_level=NOTSET
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
junit_family = xunit1
filterwarnings =
    error
    ignore::UserWarning
